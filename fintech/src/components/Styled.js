import React from "react";
import styeld from "styled-components";

const Block = styeld.div`
    border: 2px dashed #1C6EA4;
`;

const Styled = () => {
  return <Block>Styled</Block>;
};

export default Styled;
